# Benchmarks

This directory stores benchmark artifacts captured from `threadplane-bench`.

## Layout

- `baselines/local-debug/`: environment-specific baseline captures from local debug builds

These artifacts are useful for regression comparison, but they are not portable performance targets. Hardware, database state, runtime profile, and local background load all affect the numbers.
